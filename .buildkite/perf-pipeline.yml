env:
  OPENBLAS_NUM_THREADS: 1
  CLIMATEMACHINE_SETTINGS_FIX_RNG_SEED: "true"

steps:
  - label: "gpu_LES_dycoms"
    key: "gpu_LES_dycoms"
    command:
      - "mpiexec julia --color=yes --project experiments/LES/dycoms.jl"
    agents:
      config: gpu
      queue: central
      slurm_ntasks: 3
      slurm_gres: "gpu:1"

